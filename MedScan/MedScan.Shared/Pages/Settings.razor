@page "/settings"
@using MedScan.Shared.Models
@using MedScan.Shared.Services
@inject IAuthService AuthService
@inject IProfileService ProfileService
@inject IExternalNavigationService ExternalNavigationService
@inject NavigationManager Navigation

<PageTitle>Seaded</PageTitle>

@if (!AuthService.IsLoggedIn)
{
    <p>Suunan sisselogimislehele...</p>
}
else
{
    <div class="mobile-container">
        <div class="settings-content">
            <div class="settings-header">
                <span class="header-spacer"></span>
                <h1 class="page-title">Seaded</h1>
                <span class="header-spacer"></span>
            </div>

            <div class="profile-summary-card" @onclick="OpenMainProfileView">
                <div class="profile-avatar-lg main-avatar-view">
                    @GetInitials()
                </div>
                <div class="profile-summary-info main-info-view">
                    <h2>@(AuthService.CurrentUser?.FullName)</h2>
                    <p>@GetMainUserDetails()</p>
                </div>
                <div class="right-arrow">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <polyline points="9 18 15 12 9 6"></polyline>
                    </svg>
                </div>
            </div>

            <div class="settings-section">
                <span class="section-label">Rakendus</span>
                <div class="settings-card">
                    <div class="settings-item" @onclick="OpenReminderSettings">
                        <div class="icon-wrapper">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#f5a623" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 17H2a3 3 0 0 0 3-3V9a7 7 0 0 1 14 0v5a3 3 0 0 0 3 3zm-8.27 4a2 2 0 0 1-3.46 0"></path></svg>
                        </div>
                        <div class="item-text">
                            <h3>Meeldetuletused</h3>
                            <p>Teavitused ja helid</p>
                        </div>
                        <div class="right-arrow">
                            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <polyline points="9 18 15 12 9 6"></polyline>
                            </svg>
                        </div>
                    </div>
                    <div class="item-divider"></div>
                    <div class="settings-item" @onclick="OpenAppearanceSettings">
                        <div class="icon-wrapper">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#8a4baf" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path></svg>
                        </div>
                        <div class="item-text">
                            <h3>Välimus</h3>
                            <p>Teema</p>
                        </div>
                        <div class="right-arrow">
                            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <polyline points="9 18 15 12 9 6"></polyline>
                            </svg>
                        </div>
                    </div>
                    <div class="item-divider"></div>
                    <div class="settings-item" @onclick="OpenLanguageSettings">
                        <div class="icon-wrapper">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#3b82f6" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><line x1="2" y1="12" x2="22" y2="12"></line><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"></path></svg>
                        </div>
                        <div class="item-text">
                            <h3>Keel</h3>
                            <p>Eesti keel</p>
                        </div>
                        <div class="right-arrow">
                            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <polyline points="9 18 15 12 9 6"></polyline>
                            </svg>
                        </div>
                    </div>
                </div>
            </div>

            <div class="settings-section">
                <span class="section-label">Andmed</span>
                <div class="settings-card">
                    @* <div class="settings-item" @onclick="GoToMyMedicine">
                        <div class="icon-wrapper">
                           <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#258d6b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10.5 20.5l-6-6a4.95 4.95 0 1 1 7-7l6 6a4.95 4.95 0 1 1-7 7z"></path><line x1="8.5" y1="8.5" x2="15.5" y2="15.5"></line></svg>
                        </div>
                        <div class="item-text">
                            <h3>Minu ravimid</h3>
                            <p>Ava koduapteegi vaade</p>
                        </div>
                        <div class="right-arrow">›</div>
                    </div>
                    <div class="item-divider"></div> *@
                    <div class="settings-item" @onclick="OpenRavimiregisterAsync">
                        <div class="icon-wrapper">
                           <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#258d6b" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m10.5 20.5 10-10a4.95 4.95 0 1 0-7-7l-10 10a4.95 4.95 0 1 0 7 7Z"></path><path d="m8.5 8.5 7 7"></path></svg>
                        </div>
                        <div class="item-text">
                            <h3>Ravimiregister</h3>
                            <p>www.ravimiregister.ee</p>
                        </div>
                        <div class="right-arrow">
                            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <polyline points="9 18 15 12 9 6"></polyline>
                            </svg>
                        </div>
                    </div>
                    <div class="item-divider"></div>
                    <div class="settings-item" @onclick="OpenSupportSettings">
                        <div class="icon-wrapper">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#8a4baf" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"></circle><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"></path><line x1="12" y1="17" x2="12.01" y2="17"></line></svg>
                        </div>
                        <div class="item-text">
                            <h3>Abi ja tugi</h3>
                            <p>medscan.loputoo@gmail.com</p>
                        </div>
                        <div class="right-arrow">
                            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <polyline points="9 18 15 12 9 6"></polyline>
                            </svg>
                        </div>
                    </div>
                </div>
            </div>

            <button class="logout-btn" @onclick="Logout">
                <span>
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" y1="12" x2="9" y2="12"></line></svg>
                </span>
                Logi välja
            </button>
            
            <div class="version-info">
                MedScan v1.0.0 • © 2026
            </div>
        </div>
    </div>
}

@code {
    private int _mainProfileId;
    private Patient? _mainUserProfile;

    protected override async Task OnInitializedAsync()
    {
        await AuthService.InitializeAsync();
        
        if (!AuthService.IsLoggedIn)
        {
            Navigation.NavigateTo("/login");
            return;
        }

        await LoadMainProfileAsync();
    }

    private async Task LoadMainProfileAsync()
    {
        var profiles = await ProfileService.GetMyProfilesAsync();

        _mainProfileId = AuthService.CurrentUser?.DefaultProfileId
            ?? profiles.FirstOrDefault(p => p.Type.Equals("Ise", StringComparison.OrdinalIgnoreCase))?.Id
            ?? 0;

        var profile = profiles.FirstOrDefault(p => p.Id == _mainProfileId)
            ?? profiles.FirstOrDefault(p => p.Type.Equals("Ise", StringComparison.OrdinalIgnoreCase));

        _mainUserProfile = profile is null
            ? null
            : new Patient
            {
                Id = profile.Id,
                Name = profile.Name,
                BirthDate = profile.BirthDate,
                Gender = string.IsNullOrWhiteSpace(profile.Gender) ? "Määramata" : profile.Gender
            };
    }

    private string GetMainUserDetails()
    {
        if (_mainUserProfile is null)
        {
            return "Vanus puudub · Määramata";
        }

        var age = _mainUserProfile.Age > 0 ? $"{_mainUserProfile.Age} a" : "Vanus puudub";
        return $"{age} · {_mainUserProfile.Gender}";
    }

    private void OpenMainProfileView()
    {
        if (_mainProfileId > 0)
        {
            Navigation.NavigateTo($"/settings/main-profile/{_mainProfileId}");
        }
    }

    private void OpenReminderSettings()
    {
        Navigation.NavigateTo("/settings/notifications");
    }

    private void OpenAppearanceSettings()
    {
        Navigation.NavigateTo("/settings/appearance");
    }

    private void OpenLanguageSettings()
    {
        Navigation.NavigateTo("/settings/language");
    }

    private async Task OpenRavimiregisterAsync()
    {
        await ExternalNavigationService.OpenUrlAsync("https://www.ravimiregister.ee/");
    }

    private void OpenSupportSettings()
    {
        Navigation.NavigateTo("/settings/support");
    }

    private string GetInitials()
    {
        var name = AuthService.CurrentUser?.FullName;
        if (string.IsNullOrWhiteSpace(name)) return "NA";
        
        var parts = name.Split(' ', StringSplitOptions.RemoveEmptyEntries);
        if (parts.Length == 1) return parts[0].Substring(0, 1).ToUpper();
        
        return (parts[0].Substring(0, 1) + parts[^1].Substring(0, 1)).ToUpper();
    }

    private async Task Logout()
    {
        await AuthService.LogoutAsync();
        Navigation.NavigateTo("/login", true);
    }

}

